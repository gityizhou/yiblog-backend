spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yiblog?useSSL=false&serverTimezone=UTC
    username: root
    password: zxc47POI!

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.zhouyi: info
  file: log/blog-pro.log
server:
  port: 8081
